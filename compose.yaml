services:
  pixelproof:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - pixelproof-data:/data
    restart: unless-stopped

volumes:
  pixelproof-data:
